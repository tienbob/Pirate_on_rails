
<% content_for :head do %>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
<% end %>
<%= stylesheet_link_tag 'tag_show', media: 'all' %>

<div class="film-hero-section">
  <div class="floating-elements"></div>
  
  <div class="container position-relative" style="z-index: 2;">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-xl-6">
        <div class="tag-showcase-card p-5">
          <h1 class="tag-title"><%= @tag.name %></h1>
          <p class="tag-subtitle">Genre • Category • Style</p>
          
          <div class="tag-description">
            <strong>Description:</strong> <%= @tag.description || "This genre represents a unique category of films that captivates audiences with its distinctive storytelling approach and thematic elements." %>
          </div>
          
          <div class="action-buttons">
            <% if current_user&.admin? %>
              <%= link_to 'Edit Tag', edit_tag_path(@tag), class: 'film-btn film-btn-primary' %>
              <%= link_to 'Delete Tag', tag_path(@tag), method: :delete, 
                  data: { confirm: 'Are you sure you want to delete this tag?' }, 
                  class: 'film-btn film-btn-danger' %>
            <% end %>
            <%= link_to 'Back to Tags', tags_path, class: 'film-btn film-btn-secondary' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
