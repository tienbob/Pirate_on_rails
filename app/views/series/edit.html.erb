<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="glass-card p-5 mb-4">
        <h2 class="mb-4 text-white">Edit Series</h2>
        <%= render 'form', series: @series %>
        <div class="mt-3">
          <%= link_to 'Back', series_path(@series), class: 'btn btn-outline-secondary' %>
        </div>
      </div>
    </div>
  </div>
</div>
