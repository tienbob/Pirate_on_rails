<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pirate On Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/new_icon.png">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <style>
      body.bg-main-gradient {
        /* Bamboo green gradient */
        background: linear-gradient(135deg, #b4ec51 0%, #429321 100%);
        min-height: 100vh;
        font-family: 'Mochiy Pop One', 'Segoe UI', 'Arial', sans-serif; /* Apply anime font */
        color: #fff; /* Default text color */
      }
      .card, .navbar, .footer {
        border-radius: 1rem !important;
        box-shadow: 0 2px 16px rgba(106,17,203,0.10);
        background: rgba(255, 255, 255, 0.1); /* Glassmorphism effect */
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        border: none;
      }
      .navbar {
         background: rgba(0, 0, 0, 0.5); /* Semi-transparent navbar */
      }
      .navbar .nav-link, .navbar .navbar-brand {
        color: #fff !important;
      }
      .btn-gradient {
        background: linear-gradient(90deg, #ff8c00 0%, #ff0080 100%); /* Anime gradient */
        color: #fff;
        border: none;
        box-shadow: 0 2px 8px rgba(255,0,128,0.15);
        transition: background 0.3s;
      }
      .btn-gradient:hover {
        background: linear-gradient(90deg, #ff0080 0%, #ff8c00 100%);
        color: #fff;
      }
      .badge.bg-gradient {
        background: linear-gradient(90deg, #ff8c00 0%, #ff0080 100%); /* Anime gradient */
        color: #fff;
      }
      footer.bg-dark {
        background: rgba(0, 0, 0, 0.5) !important; /* Semi-transparent footer */
        color: #fff;
      }
      /* Ensure main content is above footer */
      main {
        position: relative;
        z-index: 1;
      }
    </style>
  </head>

  <body class="bg-main-gradient d-flex flex-column min-vh-100">
    <%= render 'layouts/header' %>
    <main class="flex-grow-1">
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
